export default function Loading() {
  return (
    <main className="pf-status-page">
      <section className="pf-status-card is-loading">
        <span className="pf-route-spinner" />
        <p>Loading...</p>
      </section>
    </main>
  );
}
